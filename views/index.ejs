<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
  <div class="main w-full min-h-screen bg-zinc-900">
    <div class="form p-10 text-white">
      <form action="/create" method="post">
        <input class="block w-full rounded-md bg-zinc-800 p-5" type="text" placeholder="Title" name="title">
        <textarea class="block w-full rounded-md bg-zinc-800 mt-2 p-5" id="" cols="30" rows="10"
          placeholder="Details" name="details"></textarea>
        <input class="bg-blue-600 px-5 py-2 rounded-md mt-5" type="submit" value="Create Task">
      </form>
    </div>
    <div class="tasks p-10 flex gap-4 flex-wrap">
      <% if(fileTitles.length > 0) { %>
        <% for(let i = 0; i < fileTitles.length; i++) { %>
          <div class="task w-72 bg-zinc-800 px-3 py-4 text-white hover:border-solid hover:border-2 hover:border-sky-500">
            <h1 class="text-3xl tracking-tighter"><%= fileTitles[i] %></h1>
            <a class="text-blue-500 inline-block mt-3" href="/files/<%= fileNames[i] %>">Read More..</a>
          </div>
        <% } %>
      <% } else { %>
        <h1 class="text-zinc-400 text-3xl">No Tasks Yet.</h1>
      <% } %>
    </div>
  </div>
</body>
</html> 